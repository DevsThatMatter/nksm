echo "Testing your code...🛠️"

bun format:check ||
(
    echo 'Prettier Check Failed. Run bun format, add changes and try commit again.';
    false;
)

echo 'Tests successful, Commiting the changes'